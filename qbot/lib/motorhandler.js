'use strict';

module.exports = (gpioService => {

    const _service = gpioService;

    const self = {
        list: (cmd) => {
            return {};
        },

        get: (cmd) => {
            return {};
        },

        set: (cmd) => {
            return {};
        },

        setall: (cmd) => {
            return {};
        }
    }
    return self;
});
